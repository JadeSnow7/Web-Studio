# Native VT GUI acceptance checkpoint — 2026-09-16

## Build and scope

App: `/private/tmp/web-studio-vt-unique/Build/Products/Debug/Web Studio VT.app`, bundle `com.huaodong.Web-Studio.VTValidation20260916`; build log `evidence/unique-gui-build.log`. All observations below used native Computer Use keyboard/mouse, screenshots and accessibility state. This binary predates subsequent PTY, keyboard, IME and accessibility repairs. System Appearance was restored to its original Auto setting after testing.

## Passed observed cases

- Real CoreText/Metal text and cursor display in an on-demand MTKView. Common output: ANSI green heading, Chinese `中文测试`, combining `é`, ZWJ emoji `👩‍💻`, truecolor red and inverse colors. Non-ASCII output was generated using UTF-8 octal bytes; this is not IME evidence.
- Default terminal grid: `stty size` returned 45×98. Split returned 45×47; dragging the divider returned 45×61. Same shell PID 30985 throughout.
- Native Ctrl-C interrupted `sleep 30`; the following command generated `INT_OK`.
- Double-click selected `INT_OK`; Cmd-C copied it. Cmd-V showed the explicit paste confirmation. After confirming, a command generated `COPY=[INT_OK]`.
- `seq 1 120 | less`: initial 1–44, Space advanced to 45–88, q restored the shell screen.
- `vim -Nu NONE -n`: inserted `VT_INPUT_OK`, Escape/Left/x produced `VT_INPUT_K`; `:q!` returned without saving a file.
- `top -o cpu -n 5`: live aligned output displayed; q restored the shell.
- Cmd-L selected the directory address; Cmd-K opened the terminal-targeted command palette; Escape dismissed both.
- Hidden terminal continued parsing a delayed `HIDDEN_READY` output. Switching back retained output and PID 30985.
- Wheel scrollback over `seq 1 150` showed earlier rows 9–53. Typing returned to the bottom. Native scrollbar remained disabled: a defect, not acceptance.
- Light → Dark → original Auto changed actual background/text colors without losing content or PID. Common visual output remained intact. Selection retention across theme changes was not tested.
- Isolated localhost SSH at 127.0.0.1:49222, temporary key and strict known-host checking, started `/bin/sh -i` through the new terminal. Generated `SSH_INPUT_OK`, reported 45×47, then after divider drag reported 45×61 and generated `SSH_RESIZE_OK`. `exit` closed SSH and returned to local shell. No user remote was contacted; dedicated SSH resource and first-use host-key confirmation remain untested.

- Smaller split window: generated `SMALL_PID=30985` and `stty size` 32×30 after dragging the outer window corner. Text reflowed and remained visible. Exact logical 900×560 dimensions have not been independently established.
- Real SGR mouse reporting: a temporary raw-mode Python reader enabled modes 1000/1006; native left click produced `ESC[<0;7;6M`. Shift override, drag and wheel protocol coverage remain pending.
- System IME attempt: temporarily added Apple Simplified Pinyin through System Settings. Configured Ctrl-Space and Ctrl-Option-Space events did not show a candidate/preedit window through Computer Use; SystemUIServer access timed out. This is unverified, not a passed IME test or a diagnosed app defect. Removed all temporary input sources and verified the original ABC/US/WeChat list was restored.

- Web regression checkpoint: opened `https://example.com` in the other pane; actual WebKit title/content/link rendered while terminal remained visible and focused. No Agent request or external message was sent.

- Ten-second Metal idle observation on this binary: PID30261, zero command-buffer submissions and zero GPU intervals attributed to that PID. A concurrent `ps` sample reported CPU0.0%, RSS30992KiB. Window visibility was not independently established, so this does not prove the visible-idle case or measured load performance. Aggregate and binary hash: `evidence/gui-idle-metal-checkpoint.json`. Raw trace stays outside the repository.
- The localhost SSH fixture and all task PTY smoke/resistant processes were stopped; an elevated process query found no matching remaining processes. The GUI app remains available for manual IME checks.

## Earlier checkpoint pending / defects (superseded where noted below)

Real Chinese IME preedit/candidates/commit; 900×560 window; actual alternate display scale; native scrollbar enablement/drag; TUI mouse and Shift selection; full accessibility and system preferences; final lifecycle cleanup; performance comparison; full app regression; final source rebuild and Release; production switch. Actual app screenshots were inspected in tool output but have not yet been saved as standalone PNG artifacts.

## Available display environment

A host `system_profiler SPDisplaysDataType` query found one online built-in Color LCD: 2940×1912 physical pixels, 1470×956 logical resolution at 60 Hz (2×). No alternate connected display scale was available. The renderer's 1× output remains an offscreen test, not a second physical-display acceptance. Hardware identifiers were not copied into this report.

## Final terminal checkpoint

App: `/private/tmp/web-studio-vt-final/Build/Products/Debug/Web Studio VT.app`, bundle `com.huaodong.Web-Studio.VTFinal20260916`. Two launches are distinguished below; the later shared Agent protocol isolation fix only received rebuild/unit coverage.

### Before the separate nonblinking-preference addition

- Shell PID 55205, 45×98. ANSI, truecolor, inverse, Chinese, combining and emoji output inspected in actual light/dark windows.
- `seq 1 150`: native scrollbar enabled; knob drag moved viewport from the end to rows 3–47. Typing returned to the bottom; PID remained 55205.
- Double-click selected `VT_ACCEPTANCE`; AX selected text and visible selection survived Dark → Auto.
- System increase contrast changed default foreground/native chrome while explicit colors and selection remained. Restored increase contrast off, reduce transparency off, and appearance Auto.
- Cmd-Q displayed the running-session warning; confirming Quit removed both app PID 54291 and shell PID 55205, independently checked with host `ps`.

### After the public nonblinking-preference addition

- Relaunched updated terminal app (PID 93556). Startup immediately rendered the existing user shell prompt. It was then replaced with `/bin/sh -i` for the fixture without editing shell configuration.
- Shell PID 93867, initial 45×98. `VT_FINAL`, ANSI green, truecolor, Chinese, combining `é` and ZWJ emoji rendered; native AX text preserved their text.
- Agent resources initially showed Terminal unselected. Explicitly selected it, opened preview and clicked Read. Expanded snapshot reported complete range `0..<501`, matching visible text including Unicode, PID and running state. No Agent request was sent.
- Narrower window retained content and reflowed. Exact logical 900×560 height was not established by drag, so that gate remains open.
- `printf 'FINAL_EXIT_SCREEN\n'; exit 7` retained the final screen and showed the compact `Exited (7)` status.
- Reduce motion and the separate nonblinking preference were observed off in System Settings and left unchanged. Their policy branches/renderer phase behavior passed host tests, but live timer transitions under those preferences remain unverified.

All live screenshots above remain in the Computer Use task record; no standalone GUI PNG file is claimed. Actual alternate display scale, real Chinese IME, full VoiceOver, dedicated SSH resource/host-key flow and controlled performance comparison remain pending.
